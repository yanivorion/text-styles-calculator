import React from 'react';
import ReactDOM from 'react-dom/client';
import './index.css';
import TextStylesPlayground from './text-styles-playground';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <TextStylesPlayground />
  </React.StrictMode>
);
